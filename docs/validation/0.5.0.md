# 0.5.0 candidate validation

Recorded on 2026-08-27 for commit `b34b04b0857606c079050bd200e69d00cdc657c7`.
All six public packages are versioned `0.5.0` through Changesets.

**Automated verification passed. Publication remains held for manual
assistive-technology verification.** This run did not push, publish packages,
deploy documentation, or change the developer's SeniorPath checkout.

## Reproducible checks

A separate Git clone with no workspace links installed the frozen lockfile.
`pnpm release:dry-run` passed there and left the tracked checkout clean.
Consumer applications installed the six exact tarballs in additional OS
temporary directories, not through source aliases or links.

| Check                | Result                                                                                                        |
| -------------------- | ------------------------------------------------------------------------------------------------------------- |
| Contracts            | 64 custom elements; generated adapters and API reports match                                                  |
| Package tests        | 171 elements, 4 motion, 3 React unit, 10 React browser, 3 notes storage tests passed                          |
| Browser suite        | 131 passed; 4 visual cases intentionally restricted to Chromium                                               |
| Documentation        | 61 passed; 9 desktop-only cases omitted on mobile                                                             |
| Packaging            | Six tarballs, export/type validation and budgets for 92 entrypoints passed                                    |
| Dependencies         | Audit reported no known vulnerabilities; license and install-script policies passed                           |
| SeniorPath candidate | Style guard, 15 integration unit tests, typecheck of 294 files, build of 56 pages and 13 browser flows passed |

The SeniorPath check used disposable starter/test content. It did not run the
private editorial-catalog suite or claim to validate the user's article data.
The developer checkout's diff and untracked integration files had identical
hashes before and after the check.

## Installed framework matrix

Tooling: pnpm 10.32.0 and Playwright 1.62.1 on Linux/WSL.

| Runtime      | Installed consumers                                                                                           |
| ------------ | ------------------------------------------------------------------------------------------------------------- |
| Node 24.19.0 | React 18.3.1 and 19.2.8; Astro 5.18.2 and 7.2.4; Next.js 16.3.3 with React 19.2.8; Vue 3.5.42; Svelte 5.56.10 |
| Node 22.12.0 | Additional React 18.3.1 / Astro 5.18.2 package checks and React/Vue/Svelte browser flows                      |

React/Vue/Svelte interactions passed in Chromium, Firefox and WebKit. Next.js
passed both a request-time `next start` scenario and the actual static hosting
path, including reload, navigation, forms, persistence, deletion, focus return
and error capture. The static export is not evidence of an SSR deployment.

React checks include all six form controls, external values, reset, FormData,
required/disabled states, refs, dynamic slots, consumer-owned navigation and
Strict Mode. Real clicks cover input/change ordering when an input handler
rerenders React. Async toast completion cannot revive a removed host.

Storage failures retained the current draft and existing data. Simple consumer
bundles excluded charts, Rough.js, GSAP and motion entrypoints.

## Visual and accessibility evidence

Reviewed light/dark library screenshots, desktop/mobile notes, no-JavaScript
content, keyboard creation and dialog focus return in isolated browser sessions.
The four updated Chromium baselines reflect the self-hosted Caveat font and
shared theme-contrast corrections, not a new component design.

Automated checks cover keyboard interaction, accessible names/states, axe,
200% text reflow, reduced motion, forced colors and touch targets. These are
not screen-reader sessions.

The [manual accessibility policy](../accessibility.md) still requires:

| Combination                                    | Status                                                          |
| ---------------------------------------------- | --------------------------------------------------------------- |
| NVDA with current Firefox or Chrome on Windows | Pending; no NVDA installation was available in this environment |
| VoiceOver with Safari on macOS                 | Pending; no macOS environment was available                     |
| Manual 200% zoom and Windows High Contrast     | Pending; automated reflow and forced-colors checks passed       |

Record browser/AT/OS versions, scenario, announcements, focus behavior and any
accepted limitation. Prioritize form editing/reset/validation, modal dismissal,
toast announcements, list navigation and header/footer disclosures. Do not
advertise an AT combination as verified before that record exists.

## Delivery status

Local commits and candidate artifacts are ready for the remaining manual check.
The existing Trusted Publishing workflow is unchanged. npm publication, public
documentation/miniapp verification and the SeniorPath upgrade to published
versions remain pending. Development overrides in the real SeniorPath were
deliberately left untouched until that publication.

The candidate tarball SHA-256 values from this run are:

```text
astro     ef0559071a288d06e2476e7b25d5b1696c06f86d8bc0fcc7fae11c7b26f2bef2
elements  6cb7bce78ccae18a81a97f2b31aa8d78d10149eb778b04f640684444dc6cd8e8
icons     8b85eb3c8b5533d1b51915ebbcf1622609bb6dd2cf71a661dd489d32ecded80e
motion    4eea3697e3317c82fb45531bb6355c4748dfe3d6776a72a5d56a60905367c515
react     9ab753617da78075406887d23dadbdb5575dafad6b00a486ac66c14e44cba20e
tokens    0b14ab4d098aff487e7897a55ba08593c7a34f6b0007afcae9d28c3c7a559b67
```
