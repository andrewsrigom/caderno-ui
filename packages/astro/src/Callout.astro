---
import '@caderno-ui/elements/fallback.css'

import type { CadCalloutVariant } from '@caderno-ui/elements/callout'

type Props = {
  class?: string
  heading?: string
  variant?: CadCalloutVariant
}

const { class: className, heading, variant = 'info' } = Astro.props
---

<cad-callout class={className} heading={heading} variant={variant}>
  <slot name="action" slot="action" />
  <slot name="icon" slot="icon" />
  <slot name="title" slot="title" />
  <slot />
</cad-callout>

<script>
  import '@caderno-ui/elements/callout'
</script>
