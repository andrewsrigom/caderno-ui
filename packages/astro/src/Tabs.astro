---
import '@caderno-ui/elements/fallback.css'

type Props = {
  class?: string
  defaultValue?: string
  value?: string
}

const { class: className, defaultValue, value } = Astro.props
---

<cad-tabs class={className} default-value={defaultValue} value={value}>
  <slot />
</cad-tabs>

<script>
  import '@caderno-ui/elements/tabs'
</script>
